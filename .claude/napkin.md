# Napkin Runbook

## Curation Rules
- Re-prioritize on every read.
- Keep recurring, high-value notes only.
- Max 10 items per category.
- Each item includes date + "Do instead".

## Execution & Validation (Highest Priority)
0. **[2026-09-20] Build da Vercel (web e storefront) quebra em tipos que o tsc local aceita: `form action` como função, `cache`, `useFormState`**
   Do instead: essas tipagens do React 18.3 vivem em `react/experimental` e `react-dom/experimental`; o `next/types` as referencia a partir de `node_modules/.pnpm`, resolvido pelo hoist OCULTO do pnpm (`node_modules/.pnpm/node_modules/@types/react`), que difere entre máquinas. Cada app Next tem `types/react-experimental.d.ts` com as duas referências locais — não remover. Reproduzir Vercel = `git clone` limpo + `pnpm install --frozen-lockfile` + tsc NÃO basta (passou local e quebrou lá); quando divergir, commitar um `[diag]` no script de build e ler `vercel inspect <url> --logs`. Projetos: `mall-online-web` (root `apps/web`, app.mallevo.com.br), `storefront-mallevo` (root `apps/storefront`, `*.mallevo.com.br`); integração Git = push na branch gera Preview; `vercel inspect <url> --wait --timeout 8m` espera o build. O apex `mallevo.com.br`/`www` está no projeto `mallevo-lp-consumidor` (LP), não no storefront — o saguão só aparece lá se o domínio for movido.
1. **[2026-09-12] Typecheck dos apps mobile (consumer E partner): não há `tsc` no root nem script `typecheck`**
   Do instead: `cd apps/mobile-<app> && ./node_modules/typescript/bin/tsc --noEmit`. O `npx tsc` cai no pacote-armadilha "not the tsc command". ESLint tem script `lint` mas o binário não está instalado — não perder tempo com ele. Validação de bundle real (imports de fonte/SVG): `CI=1 npx expo export --platform android --output-dir <scratch>` (~3 min, exit 0 = ok). Lib: `apps/mobile-consumer/node_modules/typescript/bin/tsc -p packages/lib/tsconfig.json`; testes `cd packages/lib && npx vitest run`. Web/storefront/admin: os tsconfigs mapeiam `react`, `react/jsx-runtime` e `react-dom` para o `node_modules/@types` do PRÓPRIO app (paths) — sem isso, os fontes de `@mallevo/lib` (que tem `@types/react` 19 para os apps Expo) puxavam tipos do React 19 e o build da Vercel quebrava (`useFormState` inexistente / `TemplateProvider` inválido). Se o erro voltar, é alguém removendo esses `paths`.
2. **[2026-08-29] Entry do mobile-consumer é `entry.js` físico — não voltar para `"main": "expo-router/entry"`**
   Do instead: com main apontando pro pacote, o Metro anuncia o bundle pelo caminho `.pnpm` com hash (muda a cada mexida no lockfile) e `/node_modules/expo-router/entry` não resolve da raiz (sem hoist) — app "não carrega". O `entry.js` (`import 'expo-router/entry'`) dá URL estável `/apps/mobile-consumer/entry.bundle`.
3. **[2026-08-14] `@react-navigation/native` não é importável nos apps mobile (pnpm não faz hoist de dep transitiva)**
   Do instead: usar os re-exports do expo-router (`useFocusEffect`, `useNavigation`, `router`). Para "está em foco?": `useFocusEffect` + estado local — não importar `useIsFocused` direto.
4. **[2026-09-12] App mobile sem `.env.local` → `supabaseUrl is required` + cascata de "Route ... is missing the required default export" e `Cannot read property 'ErrorBoundary' of undefined`**
   Do instead: os WARNs de rota e o erro de ErrorBoundary são consequência do `lib/supabase.ts` lançar no import — não caçar default exports. Copiar `.env.local.example` → `.env.local` (os 3 apps mobile usam o MESMO projeto Supabase; URL/anon key vêm do mobile-consumer) e reiniciar com `npx expo start --clear` (env é inlinado pelo babel e fica no cache do Metro).

## Shell & Command Reliability
0. **[2026-09-19] Verificar telas AUTENTICADAS do dashboard = `pnpm qa:local` (Supabase local + seed + Playwright), nunca página de QA dentro do app nem usuário em produção**
   Do instead: exige Docker daemon (`sudo systemctl start docker` — pedir ao usuário). Runbook `docs/dev/qa-local.md`; login `qa@mallevo.local` / `mallevo-qa-2026`; loja `forno-demo`. Sem Docker, o máximo honesto é `tsc` + `playwright test --list`; dizer isso explicitamente em vez de improvisar.
1. **[2026-09-19] Revisão visual do storefront sem Playwright: Firefox headless**
   Do instead: `firefox --headless --no-remote --profile <perfil-scratch> --window-size=480,900 --screenshot out.png "http://<slug>.mallevo.localhost:3002/?preset=X&categoria=Y"` (Firefox resolve `*.localhost` sozinho; servidor com `STOREFRONT_ALLOW_PREVIEW_OVERRIDE=true`). Âncoras `#id` NÃO rolam a captura — use viewport alto (480×3200) para ver a página toda. Imagens `loading="lazy"` não bloqueiam o `load` e saem vazias na captura: pôr `user_pref("dom.image-lazy-loading.enabled", false);` no `user.js` do perfil de QA. Hero nunca é lazy (LCP). Ler o PNG com a ferramenta Read.
2. **[2026-09-19] Storefront: mover rotas deixa `.next/types` obsoleto e o `tsc` acusa módulos inexistentes; `pkill -f "<trecho do comando>"` mata o próprio shell**
   Do instead: após mover páginas em `apps/storefront/app`, `rm -rf apps/storefront/.next` antes do `tsc`/`next build`. Para derrubar o `next start`, `fuser -k 3002/tcp` (nunca `pkill -f` com texto que está na própria linha de comando — exit 144). Smoke test real: `next build && next start -p 3002`, depois `curl -H "Host: <slug>.mallevo.localhost" localhost:3002/` — o `.env.local` do storefront aponta para o Supabase REAL (slugs via REST `public_catalog_stores?select=slug`), então o que se vê é produção.
1. **[2026-08-08] Downloads sequenciais de imagem (Unsplash/picsum) estagnam no sandbox**
   Do instead: paralelizar com subshells + `curl --max-time 15` e `sleep` de coleta; `curl -I` (HEAD) responde rápido para validar URLs antes.

## Domain Behavior Guardrails
00. **[2026-09-19] Embed `stores → categories` no PostgREST é AMBÍGUO: sempre `categoria:categories!stores_categoria_id_fkey(...)`**
   Do instead: `categories` tem `store_id` (seções do cardápio) E `stores.categoria_id` (nicho) — `categories(slug)` sem `!fk` dá PGRST201 e o `data` vem null em silêncio (template genérico, badge "Layout padrão", consumer real sem lojas). O mock do consumer não parseia o select, por isso nunca apareceu. Guarda: `grep -rn "categoria[s]*:categories(" apps` deve devolver vazio.
0. **[2026-09-20] Vitrine web = porte da RN em `apps/storefront/components/vitrines/<codigo>/{<codigo>-ui,Vitrine<Codigo>}.tsx` + registro em `vitrines/index.ts` (as 15 estão portadas; `VITRINES_WEB` é `Record` completo)**
   Do instead: `'use client'`, props `VitrineWebProps`, detalhe via `ProdutoModalHost` (render-prop `abrir(id)`), sacola via `_base/Sacola` (sem FAB, sem barra de 4 abas), fonte-DNA via `_base/FonteDna`, relógio via `_base/StatusAberto` (nunca "Aberto" literal), hero-carrossel via `_base/useCarrossel` + `useHeroEmCena` + `comCopiaDeLoop` (trilho `translateX(-pos·100%)`), reduzir-movimento via `_base/movimento`. Adição rápida = padrão da Passarela (`useCartStore.adicionarItem` + checar `pendingTrocaLoja` antes do flash; item com variação/`exige_receita` abre o detalhe). Botão dentro de botão é inválido: "+"/"Adicionar" são irmãos do cartão. Numeração/ordem calculada no PAI, nunca dentro do filho (render dos filhos vem depois). `Store.tempo_entrega` é NÚMERO (minutos). Vitrine nova na lib sem porte web quebra tsc + `registro.test.ts`. QA: `STOREFRONT_ALLOW_PREVIEW_OVERRIDE=true next start` + `?preset=<arquetipo>&categoria=<slug>` numa loja real.
1. **[2026-09-10] Explorar e Seguindo = a MESMA tela de reels (`components/explorar/FeedReels.tsx`)**
   Do instead: mexer em reel (player, overlay, comentários, galeria, `?post=`) → FeedReels; as rotas `(tabs)/explorar.tsx` e `(tabs)/seguindo.tsx` são casca (`titulo`, `lojas`, `aoVoltar`, `vazio`, `cabecalhoGaleria`). Seguindo = Explorar filtrado por `useSeguidas` — o usuário decidiu isso; não voltar ao feed de cards (CardPost foi removido). Rail das seguidas vive na galeria (pele escura), com `Avatar` embutido.
2. **[2026-08-18] Busca do consumidor = overlay Concierge no Início, não rota**
   Do instead: mexer em busca → `components/home/Concierge.tsx` (recentes em `store/useBuscasRecentes`). Overlay fullscreen na home esconde a tab bar via `useImersao` (mesmo mecanismo do Explorar) e permanece montado ao navegar para a loja — resultados sobrevivem à volta, sem re-focar teclado. A rota `(tabs)/buscar` não existe mais.
3. **[2026-08-08] Transições de ambiente (entrar/sair de loja) têm padrão estabelecido**
   Do instead: entrada = `SplashLoja` (accent do lojista); saída = `TransicaoMallevo` no layout raiz + store `useTransicaoSaida` (a tela que navega desmonta e mata overlays locais — o véu TEM que viver acima do navigator). Curvas compartilhadas: ENTRADA bezier(0.16,1,0.3,1), SAIDA bezier(0.3,0,1,1), SUAVE bezier(0.4,0,0.2,1). Autoplay/parallax devem respeitar `AccessibilityInfo.isReduceMotionEnabled`.
4. **[2026-08-18] Campo em superfície escura → teclado escuro**
   Do instead: todo `TextInput` sobre marquise/surfaceDark/reels leva `keyboardAppearance="dark"` (iOS) + `selectionColor`/`cursorColor` accent. No `<Input>` isso já vem junto de `fundoEscuro`. Caret accent SÓ no escuro (lima é invisível sobre branco). Android: cor do teclado é do sistema, não forçável.
5. **[2026-09-12] Telas do consumer seguem a arquitetura marquise + folha do Início (Pedidos, Acompanhamento, Perfil e Checkout já seguem) — e o mobile-partner é port 1:1 desse sistema**
   Do instead (partner): tela principal = `components/marquise/TelaMarquise` (`marquise` prop = ao vivo; children = folha) com peças de `components/marquise/Marquise.tsx` (`Statement`, `Portaria`, `PlacaVidro`, `CartaoVidro`, `MoedaVidro`, `PilulaVidro`, `PontoAoVivo`, `BarraProgresso`) e `ui/SecaoFolha`+`CartaoFolha`; os ~18 módulos de gestão (stack) ficam claros com `CabecalhoTela` e herdam o sistema via `components/Basicos.tsx` (fachada sobre `ui/`) — não reescrever módulo por módulo. Extração/regras em docs/system-design/partner/00-sistema.md. Não mexer no consumer a partir do partner.
   Do instead (consumer): tela nova/redesenhada = marquise `marquee` + `GlowNeon` (export de Marquise) com o que está "ao vivo" + folha clara (`marginTop:-24`, `radius.md`, `VidroFosco` de `components/home/VidroFosco.tsx` com `useLuzDoDia(usePreferencias.luzDoDia)`) com o acervo. Letreiros/cartões da folha = `SecaoFolha`/`CartaoFolha` de `components/ui/SecaoFolha.tsx` (NÃO redesenhar rótulo por tela); tijolo de loja com pele = `components/TijoloLoja.tsx`; statement = `.statement` + `.acento` accent itálico. Status bar clara só com `useFocusEffect` em foco. Folhas modais = `FolhaModal` (sobrelinha + letreiro, sem fio; `fundo="canvas"` para listas de cartões); folha que sobe DA MARQUISE (notificações) é do material dela: `marquee` + `GlowNeon` + cartões `marqueeGlass`. Stack screen escura (ex.: `pedido/[id]`) põe `<StatusBar style="light">` sem guarda — o expo-status-bar restaura ao desmontar. Perfil: coleções (seguidas/favoritos/endereços) = moedas de vidro na marquise; `AvatarPerfil` aceita `corAro`. Pedidos: ativos → `components/pedidos/CartaoPedidoVivo`, histórico → `PedidoCard` (recibo com tijolo da pele da loja; `stores.theme` vem de um select à parte por id — o embed de orders não traz tema). Sem chips de filtro.
6. **[2026-09-10] Luz do dia na folha da home: só na parte clara, nunca na marquise**
   Do instead: atmosfera vive em `lib/luz-do-dia.ts` (sol real de Divinópolis, 7 quadros interpolados, hook a cada minuto) e pinta no `VidroFosco` de index.tsx como véu + sol (alpha ≤ 14%); tokens em `consumerDesign.luz` (FORA de `colors` — StoreColors espelha `colors` como strings e quebra com objeto aninhado); preferência em `store/usePreferencias.ts` (Perfil → Início). O canvas não muda; a marquise não muda — o usuário aprovou exatamente isso.
7. **[2026-08-18] Tipografia, degradê e neutros da marca vêm do painel web**
   Do instead: voz Mallevo = Plus Jakarta Sans (`useFontesMarquee()`: letreiro 700, statement 600, acento 600 itálico) sobre `marquee #18181B` + glow `marqueeGlow #C1F148` (≤22% alpha, só atmosfera — interação segue no `accent #D8FF3E`). Neutros claros = família zinco fumê (`canvas #F1F1F3`, `line #E4E4E7`; tokens TAMBÉM no tailwind.config.js — manter os dois em sincronia). Folha do home leva `VidroFosco` (nuvens SVG estilo iOS: sombra marquee 7%, inkSoft 7%, info 4%, white 55%, marqueeGlow 5% — só tokens). Referência canônica: apps/web (auth)/entrar. Space Grotesk é SÓ voz de lojista (lib/store-fonts.ts).
8. **[2026-08-20] Pisos na home do consumer = diretório + corredores, monocromático**
   Do instead: apresentação dos pisos vive em `components/home/Diretorio.tsx` (placas de wayfinding, scroll ancorado via `posicaoPorPiso`/`folhaY` na TelaHome) + letreiro de corredor em `SecaoLojas` (SÓ texto desde 2026-09-10 — o usuário achou a placa com ícone carregada; não reintroduzir). Ícone de piso no diretório = ícone de linha em `ICONE_POR_PISO` (ConsumerIcon) — o emoji de `PISOS` (@mallevo/lib) é fallback web/admin, nunca renderiza no consumer. Placas claras usam o material `ui/Vidro` (BlurView só no iOS; Android simula com `glassStrong` — N blurs dimezis na home custam caro), tokens `glass*` no consumer-design. Sem matiz por piso: identidade é ícone + tipografia, acento segue só em micro-momentos.
9. **[2026-09-10] "Suas lojas / Em alta" na marquise = cartazes em retrato (VitrineCard), corredores continuam com LojaCardH claro**
   Do instead: fileira abaixo da busca = POSTS recentes dos parceiros (`carregarPosts` de lib/posts.ts: seguidas → fallback shopping; toque → `router.navigate` p/ `(tabs)/seguindo` ou `(tabs)/explorar` com `params: { post: id }` — as duas telas rolam até o post e limpam o param). Card em `components/home/VitrineCard.tsx` recebe `VitrinePost {post, theme, logoUrl}` (largura = (w − 24 − 20) / 2.45 → 2 cards + ~45% do terceiro, altura × 1.56, gutter 24, gap 10; foto em TODA a layer + sombra de leitura subindo do pé (transparente a 46% → 0.96 na base, na cor do fundo) — não cortar a foto; sem halo/fio colorido, fio neutro `marqueeLine`; hierarquia: sobrelinha = `loja_nome` no accent DA LOJA com guarda `contrastRatio ≥ 3`, título = `produto.nome` do post (senão `descricao`), sublinha = preço (senão "há N"); pílula = momento do POST (novo < 48h → success, ≥1000 curtidas → warning, vídeo → duração em vidro); `letreiro` vem por prop — não importar `useFontesMarquee` no card: cria ciclo com Marquise). Nunca rótulo editorial inventado. Home pede `banner_url` e `categoria:categories(slug, nome)`; o mock expõe `categoria.nome`. ScrollView horizontal com halo precisa de `paddingVertical` + `marginVertical` negativo ou recorta. Corredores dos pisos = fileira HORIZONTAL (snap por card) de `components/home/FachadaLoja.tsx`, com "Ver todas" no letreiro alternando para pilha VERTICAL no mesmo lugar (prop `largura`; "Recolher" devolve o scroll ao topo do piso) (LojaCardH foi removido): a fachada veste a pele da PRÓPRIA loja via `useStoreDesignFromTheme(loja.theme)` (bg, ink, accent/accentInk, `fontStyle(design.display)`, `design.radius`) — home pede `descricao, theme` no select; 3 produtos por loja vêm de `products` (limit 3, cache de módulo); Vocabulário por piso em `VOZ_POR_PISO`. Sem rating/verificado/curadoria: não há dado — não inventar.
10. **[2026-09-20] Layout por arquétipo: a decisão é a tabela `VITRINES` da lib; o consumer ainda roda em MOCK**
   Do instead: gate por `resolveVitrine(preset, categoria_slug)` da lib (tabela `VITRINES`) — nunca por slug. `EXPO_PUBLIC_USE_MOCK=true` no .env.local: `metadata.galeria/recorte/especificacoes/unidade` vêm de lib/mock/dataset.ts; o web JÁ grava (Fase 1), mas o consumer ainda não saiu do mock (Fase 4). `metadata.estoque` → usar `stock_quantity`. 5 arquétipos sem vitrine (soft, tech, market, utility, playful) caem no layout padrão. Storefront e consumer têm 16 vitrines (Fase 2b + `mesa`/heritage em 2026-09-20); vitrine nova = entrada em `VITRINES` (lib) + `Loja<X>`/`Produto<X>` no consumer (com `BarraMenu<X>`) + `[slug].tsx` dispatch + porte web + docs 05 §5.6; os testes da lib (`vitrines.test.ts`, `store-theme.test.ts`) e do storefront (`registro.test.ts`, `lib/__tests__/vitrine.test.ts`) leem o disco e a tabela — ajustar as listas de "sem vitrine" nos dois. Falta PDP própria por vitrine no web (hoje `ProductModal`).

## Storefront — saguão do apex
1. **[2026-09-20] Apex = `app/saguao/` via REWRITE no middleware (host sem slug); loja continua host-based em `app/(loja)/`**
   Do instead: rota nova do shopping → adicionar em `ROTAS_SAGUAO` (middleware) E criar a página em `app/saguao/`; dados só por views públicas em `lib/saguao.ts`; links para loja SEMPRE por `urlDaLoja(slug)` (host-aware: `mallevo.localhost:3002` no dev). Fachada de loja = `components/vitrines/_base/Fachada.tsx` (pele escopada por CSS vars inline; elementos que sobrepõem o hero precisam `relative z-[1]`). Vocabulário/agrupamento de pisos vem da lib (`agruparPorPiso`, `VOZ_POR_PISO`…), não do consumer. Smoke: `curl localhost:3002/` (apex) vs `-H "Host: <slug>.mallevo.localhost"`; screenshots Firefox em `http://mallevo.localhost:3002/`.

## Dashboard Web (módulos)
1. **[2026-09-20] Módulo novo do dashboard = pasta em `apps/web/app/(dashboard)/<rota>/` com `page.tsx` (server), `_components/` e actions em `apps/web/lib/actions/<rota>.ts` ('use server', zod, `revalidatePath`)**
   Do instead: tenant via `supabase.from('tenants').select(...).single()`; loja via `stores.eq('tenant_id').limit(1).maybeSingle()` (mono-loja) mas toda escrita recebe `store_id` explícito; primitivos `PageHeader`/`EmptyState`/`Card`/`KPI`/`showToast`; filtros por searchParams (`router.replace` + `useSearchParams`); botão primário = `background: var(--brick)`; entrada na sidebar (`buildGrupos`) E em `comandos.ts`. Upload grande = navegador direto no bucket (`createSupabaseClient` + RLS por prefixo do tenant; vídeo por `criarUploadTUS` da lib), server action só grava a linha. Contrato de posts em `@mallevo/lib` (`src/conteudo`). E2e: seed em `supabase/seed.sql` + spec em `apps/web/e2e/`; `npx playwright test --list` valida a sintaxe sem Docker.

## User Directives
0. **[2026-09-19] Minha Loja NÃO tem preview desenhado à mão — o preview é o storefront real em iframe (`/preview?draft=`)**
   Do instead: mudança visual de vitrine se vê em `components/dashboard/preview-vitrine.tsx` + `apps/storefront/app/(loja)/preview`; não reintroduzir mockup JSX (o usuário rejeitou: "não mostra a vitrine, só o arquétipo"). Moldura "App Mallevo" (2026-09-20) = mesma página com `?app=1` → `components/store/ShellApp.tsx` do storefront veste status bar + barra de menu (molde de `VITRINES`) em volta da vitrine; chrome de topo das vitrines é sempre `sticky top-0` e o shell o empurra 47px — vale para as 15 vitrines. Preview "Unable to connect" no dashboard = storefront:3002 não está de pé (`cd apps/storefront && npx next dev -p 3002`). Verificação de tela autenticada = `pnpm qa:local` (sobe web:3100 e storefront:3002 contra Supabase local), nunca página de QA no app.
1. **[2026-08-26] Toda loja do consumer mostra a barra de navegação — sem exceção de arquétipo**
   Do instead: vitrine nova = `BarraMenu<Nome>` própria no arquivo (Início/Explorar/Pedidos/Perfil, Início aceso, `sairPara` na cor da casa). Molde A = fixa com fio (`LojaEditorial`); molde B = pílula flutuante quando a página troca de cor por seção (`LojaSmash`/`LojaForno`/`LojaRitual`). Nunca a TabBar de `(tabs)/_layout.tsx` — esse arquivo é intocável. Layout sem vitrine tem `BarraMenuPadrao` em `app/loja/[slug].tsx`. Teste-guarda em `packages/lib/.../store-theme.test.ts` quebra se faltar. Revogou o "sem barra de menu inferior" de ritual/horta/forno nos docs.
2. **[2026-09-12] Ícones: nada de faísca (`spark` = cara de IA) nem tijolo colorido por tipo (cara de app genérico) nas peças da casa**
   Do instead: moeda monocromática (vidro no escuro, `canvasAlt`/`ink` no claro) com ícone de LINHA white/ink em stroke 1.8; accent só no ponto/rótulo de estado; o tipo fala por extenso. `spark` fica restrito a vitrines de arquétipo (LojaVolt/LojaSmash) e ao onboarding. Trocas feitas: notificações (package/tag/store/info/comment), "Em alta" → `trend`, "Novo" sem ícone, Luz do dia → `clock`.
3. **[2026-08-08] Design das lojas parceiras deve ser premium, não genérico "tipo iFood"**
   Do instead: ao mexer em telas de loja, replicar a linguagem do arquétipo (referências em docs/store-theme/02): foto grande, tipografia com contraste forte, zero chrome nos cards, acento só em micro-momentos.
4. **[2026-08-08] Transições de tela devem ser abstratas — sem wordmark, logo ou texto**
   Do instead: mudança de ambiente = COR + movimento. Direção aprovada: radial — círculo na cor da paleta da loja nasce do ponto do toque, cobre a tela e dissolve no canvas Mallevo (TransicaoMallevo; a tela passa cor/origem via useTransicaoSaida). Rejeitados: véu com marca "mallevo." e cortina dupla neutra. Não reintroduzir branding/texto em transições.
